/**
 * Shared AsyncStorage keys for trial onboarding / waiting room.
 * Used by DateSelection (pre-navigation handshake) and WaitingScreen (fallback).
 */
export const WAITING_ROOM_CLARITY_MOMENT_SEEN_KEY =
    "waiting_room_clarity_moment_seen_v2"
