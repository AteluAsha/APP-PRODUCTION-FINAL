# Archive – historical plans and audits

These markdown files are completed plans, audits, fix logs, and verification reports moved from the project root to reduce clutter. They have no effect on app build or runtime.

You can delete this entire `docs/archive/` folder before production if desired, or keep it for reference.
